
version: "3.9"

services:
  web:    
    build:
      context: .
      dockerfile: Dockerfile
    image: landing-page-firewatch:static
    container_name: firewatch-web
    ports:
      - "8080:80"       
    restart: unless-stopped
